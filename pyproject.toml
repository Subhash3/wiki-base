[project]
name = "wiki-base"
version = "0.1.0"
description = "A self-hosted service for immutable, cited RAG knowledge bases"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.30",
    "document-processing",
    "fastapi>=0.115",
    "graph-rag",
    "httpx>=0.28",
    "llm-providers",
    "pgvector>=0.3",
    "pydantic-settings>=2.7",
    "python-multipart>=0.0.20",
    "uvicorn[standard]>=0.34",
]

[project.scripts]
wiki-base = "wiki_base.main:run"
wiki-base-init-db = "wiki_base.database.setup:run"
wiki-base-graph-worker = "wiki_base.workers.graph_indexing_main:run"
wiki-base-worker = "wiki_base.workers.main:run"
graph-rag-visualize = "wiki_base.graph_tools:run_visualizer"
graph-rag-visualize-merge = "wiki_base.graph_tools:run_merger"

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.25",
    "ruff>=0.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/wiki_base"]

[tool.pytest.ini_options]
addopts = "-q"
asyncio_mode = "auto"
testpaths = ["tests", "packages"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC"]

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
document-processing = { workspace = true }
graph-rag = { workspace = true }
llm-providers = { workspace = true }
